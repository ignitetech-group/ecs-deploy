version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  # Maintain Python dependencies declared in setup.py / requirements-test.txt
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
