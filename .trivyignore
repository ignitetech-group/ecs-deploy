# Trivy ignore list. Format: one check id per line; trailing comments OK.
# Document the rationale here AND mirror the same dismissal in .checkov.yaml
# so both scanners agree.
#
# (No active dismissals at bootstrap. Add only with a documented reason.)
